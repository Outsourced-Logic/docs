# Update a Session

Update an existing MultiOn session with a new user command.

## Dependencies

import { Callout } from 'nextra/components'
import { Tabs, Tab } from 'nextra/components'
 
<Tabs items={['Python', 'JS']}>
  <Tab>
  ```python copy
  pip install multion
  ```
  </Tab>
  <Tab>
  ``` js copy 
  npm install multion
  ```
  </Tab>
</Tabs>

## Update Session

To update a session, you'll need to provide the session ID (sessionId) and the new input parameters.

### Required Parameters
- tabId: The unique identifier of the session you wish to update.
- input: The new or additional information for the session.
- url: The URL relevant to the updated request.


<Tabs items={['Python', 'JS', 'Curl']}>
  <Tab>
  ```curl copy
  Check login API page to know how to generate <AUTH_TOKEN>
  ```
  </Tab>
  <Tab>
  ```python copy
    response = multion.update_session(tabId, {"input": f"what is the weather today","url": "https://www.google.com"})
  ```
  </Tab>
  <Tab>
  ``` js copy 
    await multion.update_session(tabId, {"input": f"what is the weather today","url": "https://www.google.com"})
  ```
<Callout type="info" emoji="ℹ️">
 Run above code using **node**
</Callout>
  </Tab>
  <Tab>
  ```curl copy
curl --location 'https://api.multion.ai/session/{tabId}' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer <AUTH_TOKEN>' \
--data '{
    "input": "what is the weather today",
    "url": "https://www.google.com"
}'
  ```
  </Tab>
</Tabs>

- Explore additional examples and detailed API usage at the [MultiOn API repository](https://github.com/MULTI-ON/api)


