# Direct usage of API
## Dependencies

import { Callout } from 'nextra/components'
import { Tabs, Tab } from 'nextra/components'
 
<Tabs items={['JS']}>
  <Tab>
  ``` js copy 
  npm install multion
  ```
  </Tab>
</Tabs>

## Run Sample

 
<Tabs items={['JS']}>
  <Tab>
  ``` js copy 
   const multion = new Multion();
   await multion.login(); // Wait for login to complete


   response = await multion.newSession({"input": f"Weather Today","url": "https://www.google.com"});
   console.log(response)
   let sessionId = response['sessionId']
  ```
  ```js copy
  response = await multion.update_session(sessionId, {"input": f"Weather Today","url": "https://www.google.com"})
  response
  ```
<Callout type="info" emoji="ℹ️">
 Run above code using **node**
</Callout>
  </Tab>
</Tabs>

- For more examples and detailed API usage, explore the [MultiOn API repository](https://github.com/MULTI-ON/api).

