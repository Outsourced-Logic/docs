# Direct usage of API
## Dependencies

import { Callout } from 'nextra/components'
import { Tabs, Tab } from 'nextra/components'
 
<Tabs items={['Python']}>
  <Tab>
  ```python copy
  pip install multion
  ```
  </Tab>
</Tabs>

## Run Sample

 
<Tabs items={['Python']}>
  <Tab>
  ```python copy
  import multion
  multion.login()


  # Now you can make API calls like this:
  response = multion.create_session({"url": "https://www.google.com"})
  print(response)
  sessionId = response['session_id']
  ```
  ```python copy
  response = multion.step_session(sessionId, {"input": f"Weather Today","url": "https://www.google.com"})
  response
  ```
  </Tab>
</Tabs>

- For more examples and detailed API usage, explore the [MultiOn API repository](https://github.com/MULTI-ON/api).

